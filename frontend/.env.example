# Origin of the backend API. Leave empty in dev (Vite proxies /api to localhost:4000).
# In production point at the deployed backend, e.g. the Render service:
VITE_API_URL=https://dental-v3.onrender.com

# Optional map tile overrides
# VITE_MAP_TILE_URL=
# VITE_MAP_SATELLITE_URL=
